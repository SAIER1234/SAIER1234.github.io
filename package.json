{
  "name": "personal_website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "npx tailwindcss -i assets/css/main.css -o static/css/main.css --minify",
    "build:css:dev": "npx tailwindcss -i assets/css/main.css -o static/css/main.css",
    "build": "npm run build:css && hugo --minify",
    "dev": "npm run build:css:dev && hugo server -D",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.5.3",
    "postcss": "^8.5.19",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^3.4.19"
  }
}
